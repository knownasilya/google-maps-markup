import measureTypeResult from '../../../utils/measure-type-result';
import { module, test } from 'qunit';

module('Unit | Utility | measure type result', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    var result = measureTypeResult();
    assert.ok(result);
  });
});
