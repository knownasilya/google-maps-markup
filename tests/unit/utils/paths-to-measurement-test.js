import pathsToMeasurement from '../../../utils/paths-to-measurement';
import { module, test } from 'qunit';

module('Unit | Utility | paths to measurement');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pathsToMeasurement();
  assert.ok(result);
});
