import getMeasurement from '../../../utils/get-measurement';
import { module, test } from 'qunit';

module('Unit | Utility | get measurement');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = getMeasurement();
  assert.ok(result);
});
