import featureCenter from '../../../utils/feature-center';
import { module, test } from 'qunit';

module('Unit | Utility | feature center');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = featureCenter();
  assert.ok(result);
});
