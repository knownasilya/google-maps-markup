import shapeArea from '../../../utils/shape-area';
import { module, test } from 'qunit';

module('Unit | Utility | shape area');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = shapeArea();
  assert.ok(result);
});
