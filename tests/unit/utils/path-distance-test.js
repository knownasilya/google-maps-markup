import pathDistance from '../../../utils/path-distance';
import { module, test } from 'qunit';

module('Unit | Utility | path distance');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pathDistance();
  assert.ok(result);
});
