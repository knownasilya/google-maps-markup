import createCircle from '../../../utils/create-circle';
import { module, test } from 'qunit';

module('Unit | Utility | create circle');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = createCircle();
  assert.ok(result);
});
