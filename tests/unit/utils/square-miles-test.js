import squareMiles from '../../../utils/square-miles';
import { module, test } from 'qunit';

module('Unit | Utility | square miles');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = squareMiles();
  assert.ok(result);
});
