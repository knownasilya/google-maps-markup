
import { toolId } from 'dummy/helpers/tool-id';
import { module, test } from 'qunit';

module('Unit | Helper | tool id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toolId([42]);
  assert.ok(result);
});

